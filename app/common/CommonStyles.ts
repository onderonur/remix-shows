import type { StyleProps } from "@chakra-ui/react";

export const paperStyles: StyleProps = {
  boxShadow: "md",
  rounded: "md",
  overflow: "hidden",
};

export const baseTransitionStyles: StyleProps = {
  transition: "all 0.3s ease-in-out",
};
